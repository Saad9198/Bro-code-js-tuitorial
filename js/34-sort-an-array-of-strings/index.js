let fruits = ["banana","apple","orange","mango"];


fruits = fruits.sort();   //  Make them arrange in order
fruits = fruits.reverse();   // Make them reverse
// fruits = fruits.sort().reverse();  // first sort in order according to alphatbets then rearrange them in reverse order
// console.log(fruits);

for (let fruit of fruits){
    console.log(fruit);
}