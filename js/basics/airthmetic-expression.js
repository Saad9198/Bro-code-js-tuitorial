/*
    arithmetic expression is a combination of...
    operands (values, variable, etc.)
    operators (+ - * / %)
    that can be evaluated to a value
    ex. y = x + 5;


let students = 20;
// students = students + 1;
// students = students - 1;
// students = students * 2;
// students = students / 2;
// students = students % 2;
// let extraStudents = students % 3;


// students += 1; 
// students -= 1; 
// students *= 1; 
// students /= 1; 

// console.log(extraStudents)

/*
    operator precedence 
    1. parenthesis ()
    2. exponents 
    3. multiplaction and division 
    4. addition & subtaction
*/

let result = (1 + 2) * (4 + 4);

console.log(result);
